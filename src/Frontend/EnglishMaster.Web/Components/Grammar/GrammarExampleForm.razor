@using EnglishMaster.Web.Services.Grammar

<EditForm Model="Model" OnValidSubmit="SubmitAsync" class="word-form">
    <DataAnnotationsValidator />
    <ValidationSummary />

    @if (ValidationErrors.Count > 0)
    {
        <div class="alert error">
            <ul>
                @foreach (var error in ValidationErrors.SelectMany(item => item.Value))
                {
                    <li>@error</li>
                }
            </ul>
        </div>
    }

    <div class="form-grid">
        <label class="span-2">
            <span>Example / ตัวอย่างประโยค<span class="required-marker" aria-hidden="true">*</span></span>
            <InputTextArea class="input textarea" @bind-Value="Model.ExampleEn" maxlength="1000" aria-required="true" />
            <span class="muted hint">Max 1000 characters / สูงสุด 1000 ตัวอักษร</span>
            <ValidationMessage For="@(() => Model.ExampleEn)" />
        </label>

        <label class="span-2">
            <span>Thai Translation / คำแปลภาษาไทย</span>
            <InputTextArea class="input textarea" @bind-Value="Model.TranslationTh" maxlength="1000" />
            <span class="muted hint">Max 1000 characters / สูงสุด 1000 ตัวอักษร</span>
            <ValidationMessage For="@(() => Model.TranslationTh)" />
        </label>

        <label class="span-2">
            <span>Thai Explanation / คำอธิบายภาษาไทย</span>
            <InputTextArea class="input textarea" @bind-Value="Model.ExplanationTh" maxlength="2000" />
            <span class="muted hint">Max 2000 characters / สูงสุด 2000 ตัวอักษร</span>
            <ValidationMessage For="@(() => Model.ExplanationTh)" />
        </label>

        <label>
            <span>Sort Order / ลำดับ</span>
            <InputNumber class="input" @bind-Value="Model.SortOrder" />
            <ValidationMessage For="@(() => Model.SortOrder)" />
        </label>

        <label class="checkbox-row">
            <InputCheckbox @bind-Value="Model.IsCorrectExample" />
            <span>Correct Example / ตัวอย่างที่ถูกต้อง</span>
        </label>

        @if (ShowActiveToggle)
        {
            <label class="checkbox-row">
                <InputCheckbox @bind-Value="Model.IsActive" />
                <span>Active / ใช้งาน</span>
            </label>
        }
    </div>

    <div class="actions">
        <button class="button primary" type="submit" disabled="@IsSaving">@SubmitText</button>
        <button class="button" type="button" @onclick="CancelAsync">Cancel / ยกเลิก</button>
    </div>
</EditForm>

@code {
    [Parameter, EditorRequired]
    public GrammarExampleFormModel Model { get; set; } = new();

    [Parameter]
    public IReadOnlyDictionary<string, string[]> ValidationErrors { get; set; } =
        new Dictionary<string, string[]>();

    [Parameter]
    public bool IsSaving { get; set; }

    [Parameter]
    public bool ShowActiveToggle { get; set; }

    [Parameter]
    public string SubmitText { get; set; } = "Save";

    [Parameter]
    public EventCallback OnValidSubmit { get; set; }

    [Parameter]
    public EventCallback OnCancel { get; set; }

    private async Task SubmitAsync()
    {
        await OnValidSubmit.InvokeAsync();
    }

    private async Task CancelAsync()
    {
        await OnCancel.InvokeAsync();
    }
}
