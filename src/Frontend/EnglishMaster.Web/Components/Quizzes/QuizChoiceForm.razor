@using EnglishMaster.Web.Services.Quizzes

<EditForm Model="Model" OnValidSubmit="SubmitAsync" class="word-form">
    <DataAnnotationsValidator />
    <ValidationSummary />

    @if (ValidationErrors.Count > 0)
    {
        <div class="alert error">
            <ul>
                @foreach (var error in ValidationErrors.SelectMany(item => item.Value))
                {
                    <li>@error</li>
                }
            </ul>
        </div>
    }

    <div class="form-grid">
        <label class="span-2">
            <span>Choice / ตัวเลือก</span>
            <InputText class="input" @bind-Value="Model.ChoiceText" />
            <ValidationMessage For="@(() => Model.ChoiceText)" />
        </label>

        <label class="checkbox-row">
            <InputCheckbox @bind-Value="Model.IsCorrect" />
            <span>Correct / คำตอบถูก</span>
        </label>

        <label>
            <span>Sort Order / ลำดับ</span>
            <InputNumber class="input" @bind-Value="Model.SortOrder" />
            <ValidationMessage For="@(() => Model.SortOrder)" />
        </label>

        <label class="span-2">
            <span>Thai Explanation / คำอธิบายภาษาไทย</span>
            <InputTextArea class="input textarea" @bind-Value="Model.ExplanationTh" />
            <ValidationMessage For="@(() => Model.ExplanationTh)" />
        </label>

        <label class="span-2">
            <span>English Explanation / คำอธิบายภาษาอังกฤษ</span>
            <InputTextArea class="input textarea" @bind-Value="Model.ExplanationEn" />
            <ValidationMessage For="@(() => Model.ExplanationEn)" />
        </label>

        @if (ShowStatusToggle)
        {
            <label class="checkbox-row">
                <InputCheckbox @bind-Value="Model.IsActive" />
                <span>Active / ใช้งาน</span>
            </label>
        }
    </div>

    <div class="actions">
        <button class="button primary" type="submit" disabled="@IsSaving"><Icon Name="plus" Size="15" /> @SubmitText</button>
        @if (OnCancel.HasDelegate)
        {
            <button class="button" type="button" @onclick="CancelAsync">Cancel / ยกเลิก</button>
        }
    </div>
</EditForm>

@code {
    [Parameter, EditorRequired]
    public QuizChoiceFormModel Model { get; set; } = new();

    [Parameter]
    public IReadOnlyDictionary<string, string[]> ValidationErrors { get; set; } =
        new Dictionary<string, string[]>();

    [Parameter]
    public bool IsSaving { get; set; }

    [Parameter]
    public bool ShowStatusToggle { get; set; }

    [Parameter]
    public string SubmitText { get; set; } = "Save";

    [Parameter]
    public EventCallback OnValidSubmit { get; set; }

    [Parameter]
    public EventCallback OnCancel { get; set; }

    private async Task SubmitAsync()
    {
        await OnValidSubmit.InvokeAsync();
    }

    private async Task CancelAsync()
    {
        await OnCancel.InvokeAsync();
    }
}
