# v0.3.0 Production Go-Live Checklist

## Status

`Prepared - Not Approved`

## Required Before Go-Live

- [ ] Release owner approves v0.3.0 production go-live. *(Human decision — not evaluable in code.)*
- [x] Staging UAT passes. Evidence: `docs/testing/v0.3.0-staging-validation-results.md`, `docs/testing/v0.3.0-uat-execution-record.md` — all 10 defects found during validation are fixed; no open Blocker/High defects.
- [x] No Blocker defects remain open. All 8 fixed and pushed (`5d30fc0`) — `docs/testing/v0.3.0-defect-log.md`.
- [x] No High security, privacy, delivery-secret, public exposure, or cross-user data defects remain open. V030-DEF-009 (High) resolved — `docs/testing/v0.3.0-defect-log.md`. Security spot checks all passed — `docs/testing/v0.3.0-staging-validation-results.md`.
- [x] Backup and restore plan is verified. `docs/release/v0.3.0-rollback-plan.md` + real drill evidence in `docs/testing/v0.3.0-backup-restore-verification.md`. Caveat: local dry run, not production-scale.
- [ ] Rollback owner is assigned. *(Human/organizational decision — not evaluable in code.)*
- [ ] Final tag `v0.3.0` is created only after approval. *(Process gate — pending release owner.)*
- [x] Production configuration is reviewed without exposing secrets. See Configuration Checks below — reviewed 2026-07-18, no secrets found in tracked files.

## Configuration Checks

- [x] Production database connection is configured through secrets or environment configuration. Verified: no `appsettings.Production.json` is tracked, only `appsettings.Production.example.json` with placeholder values (`<sql-user>`, `<sql-password>`). `Program.cs` fails fast (`InvalidOperationException`) if `ConnectionStrings:DefaultConnection` is empty in Production/Staging. Documented as required in `docs/deployment/production-environment-variables.md`.
- [x] Email provider credentials are configured outside the repository. Verified: `appsettings.Production.example.json`/`appsettings.Staging.example.json` use `<set-from-secret>` placeholders; base `appsettings.json` has an empty `Smtp.Password`. No hardcoded credential in any tracked file.
- [ ] Email provider is approved for production sending. **Real gap, not just a checkbox:** `Email__Provider` defaults to `Development` and no real SMTP/provider adapter exists in the codebase yet (`docs/deployment/production-environment-variables.md`: *"`Development` until a real provider adapter is added"*). There is nothing to "approve" yet — a production email provider adapter needs to be built before this can be checked. Flagging this as a blocker for real email delivery in production, independent of the go-live tag itself.
- [x] Public certificate verification URL is configured. **No dedicated config key exists, and none is needed** — public certificate verification is exposed as a relative route (`GET /api/v1/public/certificates/{verificationCode}` and the Web page `/certificates/verify/{code}`), resolved against whatever public hostname serves the app. Operator action needed is limited to ensuring `AllowedHosts`/DNS/routing for the public hostname is correct in production — not a certificate-specific setting.
- [x] Health endpoints are available. `/health`, `/health/live`, `/health/ready` exist on both API and Web, verified repeatedly this cycle including against a restored database.
- [ ] Logging and monitoring are enabled. **Partial, real gap:** only default ASP.NET Core logging is wired up (no Serilog/Application Insights/OpenTelemetry). `docs/operations/production-monitoring.md` explicitly lists alerting (health-check-fail alerts, Slack/Teams webhooks, uptime monitors) and an admin health dashboard as **TODO, not implemented**. Health endpoints exist, but log aggregation and alerting depend entirely on the hosting platform/container runtime the operator chooses — there is no in-app APM.

## Deployment Checks

- [ ] Confirm deployed artifact/revision. *(Only meaningful at actual deploy time.)*
- [x] Apply reviewed EF migrations if required. All 30 migrations (including this cycle's 3 cascade-path fixes and 2 corrective additive migrations) verified to apply cleanly from an empty database — see `docs/testing/v0.3.0-defect-log.md` and the new `FreshDatabaseMigrationTests` regression test.
- [x] Confirm app startup. Verified repeatedly against LocalDB, including the restore drill.
- [x] Confirm database connectivity. `/health/ready` verified passing multiple times this cycle.
- [ ] Confirm background delivery workers are in expected mode. **Real gap, not just a checkbox:** there is no `IHostedService`/`BackgroundService` for email/notification delivery anywhere in the codebase. `ProcessPendingEmailQueueCommand` only runs when `POST /api/v1/admin/email-delivery/process` is called manually or by an external scheduler — there is no automatic polling in any environment, including Production. **Operational decision needed:** either accept manual queue processing, or set up an external cron/scheduled task hitting that endpoint before go-live, since nothing will otherwise deliver queued notifications automatically.
- [x] Confirm rollback package and database backup are available. Rollback plan and a real backup/restore drill both exist and are evidenced — see `docs/release/v0.3.0-rollback-plan.md` and `docs/testing/v0.3.0-backup-restore-verification.md`. Availability of an actual previous-release deployable *artifact* is an operational/CI-pipeline matter outside this repo's scope.

## Go/No-Go Decision

Current recommendation: `No-Go`.

Engineering-verifiable items are now complete (staging UAT, defect closure, backup/restore, migrations, health checks, secret hygiene). Production deployment remains blocked on:
1. Two real operational/engineering gaps surfaced by this review: **no production email provider adapter exists** (only a `Development` stub), and **no background worker or external scheduler drives email/notification delivery** — queued items only get processed when someone or something calls the admin endpoint. Both need a decision (build vs. accept manual/external triggering for this release) before real production email delivery can be trusted.
2. Human/organizational sign-offs: release owner approval, rollback owner assignment, and logging/monitoring/alerting setup on whatever hosting platform is chosen (none of this is in-repo).
3. Docker Compose staging re-validation remains deferred per the release owner's own instruction — not currently blocking, but still an open item before this checklist can claim full staging parity.
