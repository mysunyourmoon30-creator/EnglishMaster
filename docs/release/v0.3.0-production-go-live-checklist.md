# v0.3.0 Production Go-Live Checklist

## Status

`Prepared - Not Approved`

## Required Before Go-Live

- [ ] Release owner approves v0.3.0 production go-live. *(Human decision — not evaluable in code.)*
- [x] Staging UAT passes. Evidence: `docs/testing/v0.3.0-staging-validation-results.md`, `docs/testing/v0.3.0-uat-execution-record.md` — all 10 defects found during validation are fixed; no open Blocker/High defects.
- [x] No Blocker defects remain open. All 8 fixed and pushed (`5d30fc0`) — `docs/testing/v0.3.0-defect-log.md`.
- [x] No High security, privacy, delivery-secret, public exposure, or cross-user data defects remain open. V030-DEF-009 (High) resolved — `docs/testing/v0.3.0-defect-log.md`. Security spot checks all passed — `docs/testing/v0.3.0-staging-validation-results.md`.
- [x] Backup and restore plan is verified. `docs/release/v0.3.0-rollback-plan.md` + real drill evidence in `docs/testing/v0.3.0-backup-restore-verification.md`. Caveat: local dry run, not production-scale.
- [x] Rollback owner is assigned. `chotikku` (release owner, solo-owner project) — assigned 2026-07-18.
- [x] Final tag `v0.3.0` is created only after approval. Release owner approved 2026-07-18; tag created and pushed the same day — see `docs/release/v0.3.0-final-tag-execution.md`.
- [x] Production configuration is reviewed without exposing secrets. See Configuration Checks below — reviewed 2026-07-18, no secrets found in tracked files.

## Configuration Checks

- [x] Production database connection is configured through secrets or environment configuration. Verified: no `appsettings.Production.json` is tracked, only `appsettings.Production.example.json` with placeholder values (`<sql-user>`, `<sql-password>`). `Program.cs` fails fast (`InvalidOperationException`) if `ConnectionStrings:DefaultConnection` is empty in Production/Staging. Documented as required in `docs/deployment/production-environment-variables.md`.
- [x] Email provider credentials are configured outside the repository. Verified: `appsettings.Production.example.json`/`appsettings.Staging.example.json` use `<set-from-secret>` placeholders; base `appsettings.json` has an empty `Smtp.Password`. No hardcoded credential in any tracked file.
- [x] Email provider is approved for production sending. **Correction to this checklist's earlier (wrong) assessment:** the previous entry here claimed no production adapter existed, based on a stale line in `docs/deployment/production-environment-variables.md` rather than the actual source — a real SMTP adapter (`src/Backend/EnglishMaster.Infrastructure/Notifications/SmtpEmailSender.cs`) already existed. It has since been modernized from the obsolete `System.Net.Mail.SmtpClient` (`SYSLIB0014`) to MailKit, with unit test coverage added (`tests/EnglishMaster.UnitTests/Notifications/SmtpEmailSenderTests.cs`). Set `Email__Provider=Smtp` plus `Email__Smtp__*` secrets to activate it in production — see the corrected `docs/deployment/production-environment-variables.md`. "Approval" of the *sending domain/account itself* (SPF/DKIM, sender reputation, provider account) remains an operational task outside this repo.
- [x] Public certificate verification URL is configured. **No dedicated config key exists, and none is needed** — public certificate verification is exposed as a relative route (`GET /api/v1/public/certificates/{verificationCode}` and the Web page `/certificates/verify/{code}`), resolved against whatever public hostname serves the app. Operator action needed is limited to ensuring `AllowedHosts`/DNS/routing for the public hostname is correct in production — not a certificate-specific setting.
- [x] Health endpoints are available. `/health`, `/health/live`, `/health/ready` exist on both API and Web, verified repeatedly this cycle including against a restored database.
- [ ] Logging and monitoring are enabled. **Partial:** email-based threshold alerting (`SystemHealthWorker`) is built and verified — see `docs/release/v0.3.0-monitoring-execution-record.md`. Structured application logging (Serilog/OpenTelemetry), Slack/Teams webhook alerts, and an external uptime monitor are explicitly deferred to v0.4.0's approved "production hardening and operational automation" scope (`docs/roadmap/v0.4.0-scope-lock.md`) rather than left as an undecided v0.3.0 gap — full log aggregation also depends on whichever hosting platform is chosen.

## Deployment Checks

- [ ] Confirm deployed artifact/revision. *(Only meaningful at actual deploy time.)*
- [x] Apply reviewed EF migrations if required. All 21 migrations (including this cycle's 3 cascade-path fixes and 2 corrective additive migrations) verified to apply cleanly from an empty database — see `docs/testing/v0.3.0-defect-log.md` and the new `FreshDatabaseMigrationTests` regression test. (Corrects an earlier miscount in this checklist that said "30" — verified via `dotnet ef migrations list`, which returns exactly 21.)
- [x] Confirm app startup. Verified repeatedly against LocalDB, including the restore drill.
- [x] Confirm database connectivity. `/health/ready` verified passing multiple times this cycle.
- [x] Confirm background delivery workers are in expected mode. **Resolved:** `EmailDeliveryWorker` (`src/Backend/EnglishMaster.Infrastructure/Notifications/EmailDeliveryWorker.cs`), a `BackgroundService` registered in the API host, now polls the pending email queue automatically (default every 60s, configurable via `EmailDeliveryWorker__PollingInterval`, can be disabled via `EmailDeliveryWorker__Enabled=false`). Verified with a dedicated integration test (`tests/EnglishMaster.IntegrationTests/Notifications/EmailDeliveryWorkerTests.cs`) that queues an email and confirms it is sent automatically with **no** call to the manual admin endpoint. The manual endpoint (`POST /api/v1/admin/email-delivery/process`) remains available for on-demand processing. No auto-retry of `Failed` messages by design — that stays a deliberate manual action.
- [x] Confirm rollback package and database backup are available. Rollback plan and a real backup/restore drill both exist and are evidenced — see `docs/release/v0.3.0-rollback-plan.md` and `docs/testing/v0.3.0-backup-restore-verification.md`. Availability of an actual previous-release deployable *artifact* is an operational/CI-pipeline matter outside this repo's scope.

## Go/No-Go Decision

Current recommendation: `No-Go`.

All engineering-verifiable items are now complete (staging UAT, defect closure, backup/restore, migrations, health checks, secret hygiene, email provider adapter, background delivery worker, certificate operational checklist, rollback owner). Hosting platform is also now decided (Linux VPS + Docker Compose, 2026-07-18 — see `docs/release/v0.3.0-production-deployment-record.md` and the runbook at `docs/deployment/production-vps-deployment.md`). Production deployment remains blocked only on items outside engineering's control:
1. No VPS has been provisioned yet — an operator action requiring a real hosting account and payment, not repo work. Release owner has deliberately not yet approved production go-live itself (distinct from the tag and v0.4.0 scope approvals already given), pending an actual successful deployment. Structured logging/full alerting beyond the existing email-based threshold alerts is a deliberate v0.4.0 deferral, not an open v0.3.0 gap.
2. Docker Compose staging re-validation remains deferred per the release owner's own instruction — not currently blocking, but still an open item before this checklist can claim full staging parity.
3. Real SMTP credentials and sending-domain approval (SPF/DKIM, provider account, sender reputation) must be provisioned operationally before `Email__Provider=Smtp` is set in production — the adapter and worker are code-complete and tested, but nobody has configured a live provider account yet.
