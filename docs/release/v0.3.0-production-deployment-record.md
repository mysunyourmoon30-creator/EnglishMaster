# v0.3.0 Production Deployment Record

## Status

`Blocked - Not Deployed` (hosting platform decided; awaiting server provisioning)

## Hosting Platform

`Linux VPS + Docker Compose` — decided 2026-07-18. Uses `docker-compose.production.yml` (repo root) and the runbook at `docs/deployment/production-vps-deployment.md`. Rationale: local-path-only file storage, Blazor Server's long-lived connections, always-on background workers, and a SQL Server dependency all fit a VPS with no new infrastructure work. (Azure App Service was considered as a managed alternative but not chosen.)

## Environment

| Field | Value |
| --- | --- |
| Production URL | TBD — no server provisioned yet |
| Release version | v0.3.0 |
| Final tag | Created and pushed 2026-07-18 — see `docs/release/v0.3.0-final-tag-execution.md` |
| Deployment revision | TBD |
| Deployment owner | `chotikku` (also rollback owner — see `docs/release/v0.3.0-production-go-live-checklist.md`) |
| Deployment start time | TBD |
| Deployment end time | TBD |

## Current Blocker

No VPS has been provisioned yet — that requires an operator (not this repo's engineering work) to sign up with a hosting provider and create a server, since it costs real money and needs an account. Release-owner go-live approval is also not yet given (deliberately withheld until an actual deployment succeeds — see the Go/No-Go section of `docs/release/v0.3.0-production-go-live-checklist.md`). The deployment runbook itself is ready to execute once a server exists.

## Pre-Deploy Checklist

- [x] Final tag exists and is approved. `v0.3.0`, created and pushed 2026-07-18 — `docs/release/v0.3.0-final-tag-execution.md`.
- [ ] Backup is complete. *(Applies to the production database itself — nothing to back up until a server and database exist.)*
- [x] Rollback owner is assigned. `chotikku` — see `docs/release/v0.3.0-production-go-live-checklist.md`.
- [ ] Production secrets are configured outside the repository. *(Documented in `docs/deployment/production-vps-deployment.md` step 4 — not yet configured because no server exists to configure them on.)*
- [ ] Email provider production sending is approved. *(SMTP adapter is code-complete and tested; sending-domain/account approval is a separate operational step not yet taken.)*
- [ ] Health check URLs are known. *(Paths are fixed — `/health`, `/health/live`, `/health/ready` — but the actual production domain doesn't exist yet.)*

## Deployment Steps

| Step | Status | Evidence |
| --- | --- | --- |
| Deploy artifact | Not Run | TBD |
| Apply migrations | Not Run | TBD |
| Start application | Not Run | TBD |
| Verify database connectivity | Not Run | TBD |
| Verify background delivery mode | Not Run | TBD |
| Verify health endpoints | Not Run | TBD |

## Result

Production deployment has not been executed. Unlike the earlier blocked state, every in-repo prerequisite is now ready: the hosting platform is decided, `docker-compose.production.yml` and the step-by-step runbook exist, the tag is approved, and a rollback owner is assigned. What remains is entirely outside this repo's engineering scope — provisioning a real VPS (an operator action requiring a hosting account and payment) and the release owner's go-live approval once that deployment succeeds.
