# v0.2.0 Smoke Test Results

## Test Date

2026-07-15

## Environment

Local Windows development workspace using the .NET SDK, PowerShell, and test environment configuration. No deployed browser/UAT environment was exercised.

## Commands Executed

```powershell
dotnet restore
dotnet build --no-restore
dotnet test --no-restore
```

Additional checks:

```powershell
scripts discovery
Razor @page route verification
integration test coverage review for smoke-critical flows
```

No smoke, health-check, or release-validation script exists in `scripts/`. `scripts/run-codex-queue.ps1` was intentionally not run.

## Build Result

Passed. `dotnet build --no-restore` completed successfully.

NU1900 warnings appeared because the NuGet vulnerability feed was unavailable in the restricted environment.

## Test Result

Passed.

```text
Unit: 189 passed
Integration: 169 passed
Architecture: 7 passed
```

## Smoke Test Checklist

| Item | Result | Evidence |
| --- | --- | --- |
| Login flow exists | Passed | Web `/login` route and API auth tests exist. |
| Admin dashboard route | Passed | `/admin` Razor route compiled. |
| Public home | Passed | `/` Razor route compiled. |
| Public search | Passed | `/search` route and public search integration tests. |
| Dictionary route | Passed | `/dictionary` Razor route compiled. |
| Course discovery route | Passed | `/learn/courses` Razor route compiled. |
| Lesson discovery route | Passed | `/learn/lessons` Razor route compiled. |
| Quiz practice | Passed | `/learn/practice` route and practice tests. |
| Student dashboard | Passed | `/learn/dashboard` route compiled. |
| Practice session | Passed | Practice session integration tests. |
| Study plan | Passed | Study plan integration tests. |
| Recommendations | Passed | Learning recommendation integration tests. |
| Achievements | Passed | Motivation and achievement tests. |
| Weekly report | Passed | Learning report integration tests. |
| Admin content workflow | Passed | Existing admin module integration tests. |
| Publishing preview | Passed | `/admin/publishing` route and publishing integration tests. |
| Import validation | Passed | Import integration tests, including path-like filename rejection. |
| Content quality check | Passed | Content quality tests and routes compile. |
| Bulk operation | Passed | Bulk operation tests and routes compile. |
| Reports | Passed | Report tests and protected endpoints. |
| Notifications | Passed | Notification tests and protected endpoints. |
| Health checks | Passed | API and Web health routes compile; manual deployed health call still required. |

## Passed Items

- Restore, build, and test succeeded.
- Expected public/admin routes compile.
- Critical security and privacy paths are covered at code/test level.
- Import upload path safety is covered.
- No automated smoke blocker found.

## Failed Items

None in automated checks.

## Blockers

None found by automated smoke checks.

## High Issues

- Manual browser smoke test has not been executed in a deployed or locally hosted browser session.

## Medium Issues

- Manual auth/permission checks with non-superadmin users are still required.
- Health checks were verified by route/build coverage, not by a running deployed endpoint call.

## Low Issues

- Public discovery routes are route-stabilization entry points that currently direct users toward search.

## Screens / Manual Verification Still Required

- Login/logout in browser.
- Admin dashboard and navigation in browser.
- Learner dashboard, practice, goals, study plan, motivation, achievements, reports in browser.
- Public search and public discovery pages in browser.
- Health endpoint calls against the target deployment.
- Permission-limited user checks outside Testing mode.

## Final Smoke Test Status

Passed with issues.

Automated checks passed, but manual browser smoke and deployed health checks remain required before release sign-off.
